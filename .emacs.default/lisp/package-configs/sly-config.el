(use-package sly
  
  :init
  ;(setq inferior-lisp-program "sbcl")
  (setq inferior-lisp-program "clisp")
  )  ;; or whatever Lisp you use (e.g., ccl, ecl)

(provide 'sly-config)
;;;end sly-config.el
