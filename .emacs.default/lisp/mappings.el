;;(defun reload-config ()
;;  "Reloads the Emacs configuration file."
;;  (interactive)
;;  (load-file "~/.emacs.default/init.el"))
;; Evaluate region function for Lisp modes




(provide 'mappings)
;;;end mappings.el
